# 小鹅通接入记录

2026-09-06 在用户登录的 Chrome 中检查了真实页面，未上传图片或保存题目。

- 题库入口：`https://admin.xiaoe-tech.com/t/exam/questionBankIndex#/questionBankIndex/questionIndex?intoQuestionBank=true`
- 添加单选题：添加题目 → 单选题。编辑页 URL 为 `#/questionBankIndex/addQuestion?type=0&isEdit=false`。
- 题干、选项 A/B/C/D、解析分别使用独立的富文本 iframe。工具栏“图片”有“本地上传”“从素材库选择”两个入口。
- 素材选择弹窗含“上传图片”、分组树、文件名搜索、图片列表、确认按钮。需等待素材加载完成，不能把初始的“0 条”当成真实为空。
- 单选题每个选项旁有“设为正确答案”；保存前需选择必填题库分类。
- 解答题在平台称为“问答题”；填空题的实际编辑结构尚待检查。
- 本地 `upload-queue.json` 记录待上传文件和题号，不含账户密码。不把文件选择成功当成上传成功，不把图片上传成功当成题目保存成功。

截至 2026-09-06 程序仅完成队列准备。原待办：用户选择目标分类、建立批次素材分组、上传回执校验、同名文件唯一性校验、按题型填写和保存回执、失败重试与断点恢复。动态 edui 数字 ID 不能硬编码。


2026-09-07 继续检查并实现试用执行器：

- 用户指定试传分类 `TsetPaper-Rong`。
- 填空题编辑地址 `type=4`，答案是文本输入框，输入后按 Enter 保存。不能用答案截图代替可判分文字。当前适配单空。
- 问答题编辑地址 `type=2`，题干、必填答案、解析为三个独立编辑框。无单独答案时，用完整解答图片填答案。
- 新增 `scrollferry/xiaoe.py`，使用独立 Chrome 登录目录，支持素材上传、完整文件名搜索、截图插入与保存后页面验证。
- 队列增加文件指纹、批次唯一文件名、每张素材/每道题独立状态；对未确认的保存停止重试。已保存题不再提交。旧格式队列可以升级，未完成导出不能上传。
- GUI 增加目标分类、试传下一题、上传全部可用题目、待补全列表；重启可通过 manifest.json 恢复旧批次。
- 本地测试覆盖六月数学 PDF 回归、断点恢复与不确定提交防重；真实试传结果另行记录，不把本地测试当成后台成功。
- 素材阶段支持每批最多 100 张图片；选择题只适配默认四选项，复杂多空与公式输入尚待适配。

2026-09-07 14:28 完成第一题真实保存：

- 试传批次 `六月数学_上传试验_20260907_001753_fd2dfded`，Q0001，6 张素材。
- 先前批量上传成功；用户也在素材库确认 6 张截图。本次复用素材，不重复上传。
- 修复上传完成弹窗未关闭导致的文件名重复判断、隐藏编辑器副本计数、素材行查询范围及自定义复选框点击。
- 题干、A–D 四个选项、解析共 6 张图逐项插入并检查编辑器图片数量，答案 C，保存后返回题库列表。
- 通过另一个后台标签页重新读取，`TsetPaper-Rong` 显示 1 题，总题量由 2205 变为 2206，新题更新时间 2026-09-07 14:28:22。
- 队列 Q0001 为 saved，Q0002 为 pending。填空和问答题实际保存仍待验证。

2026-09-08 按用户指定改为独立素材中心流程：

- 导出即添加试卷名与批次前缀，清单、上传文件名与搜索名一致。每份批次的 `batch.json` 保存前缀，上传队列版本升级为 3；版本 2 的既有回执保留，不自动迁移已上传素材。
- 素材中心地址 `https://admin.xiaoe-tech.com/t/material-center/materialCenter#/materialCenter/imageList`。全部图片旁的 `img addgroup` 新建分组；分组名称输入后点确定。左侧分组搜索需要按 Enter 执行，匹配后点击分组才切换目标。
- 右侧文件搜索占位符为“请输入图片名称”；题目编辑器的素材弹窗中则为“图片名称”。均在图片表格行内核对完整文件名，避免弹窗和提示文字干扰。
- 素材中心“上传图片”是文字控件，打开带标题“上传图片”的 dialog；“请选择”输入框显示上传目标分组，选择文件前校验与预期分组一致；支持多选，每批最多 100 张。
- 已生成本地命名批次 `六月份数学三阶段测试题答案解析_20260908_105413_e1f2fbb2`，90 张图；已在真实后台新建并选中分组 `六月份数学三阶段测试题答案解析_fd094a06`。
- 本次 CUA 批量选择文件返回 Not allowed；浏览器文档要求为 ChatGPT 扩展启用 Allow access to file URLs。尚未选入文件，确认上传按钮禁用，分组为 0 张。未把失败记录为成功，整卷上传及随后搜索插图仍需实测。
- 本地测试覆盖整卷上传先于试传、待补全题目的素材也集中上传、仅素材模式不保存题目，以及导出与上传名称一致。

2026-09-08 文件权限开启后实测：

- CUA 向上述分组选择全部 90 张命名截图，点击“确认上传(90)”，收到“上传成功 90”，关闭弹窗后分组素材总量为 90。上传队列同步保存此回执。
- 真实后台已新建同名题库分类 `六月份数学三阶段测试题答案解析_fd094a06`，并在空白单选题表单通过“重新选择”选中它、点击确定，页面显示该分类。此次未保存新题。
- 软件按素材分组自动确定题库分类，先检查同名分类、缺少才创建；每道题检查分类。UI 分类名称只读。修复选择文件后上传按钮带数量导致的定位问题。
- 本次真实操作由 CUA 验证，软件上传适配器的整卷到逐题保存全流程仍需集成验证；填空及问答题实际保存仍待验证。

2026-09-08 软件适配器整卷录题测试：

- 已通过软件的真实 Chrome 流程保存 17 题到同名分类：Q0001–Q0010 单选题、Q0015 填空题（答案 6）、Q0017–Q0022 问答题。逐题确认图片插入数量、分类及保存后回到列表，队列已记 saved。已有 90 张素材全部复用。
- 实测发现素材选择弹窗首次列表请求可能覆盖搜索结果，改为等待加载并在 30 秒内重查完整文件名；匹配不到时停止，不选错图。填空题答案框改为精确占位符“请输入第1空答案”。中断后已保存题目自动跳过。
- 5 道公式填空题 Q0011、Q0012、Q0013、Q0014、Q0016 被自动审批要求用户确认补录答案，仍为 blocked，未保存。候选答案分别为 1/6、9π/2、e、-1/6、1/2；Q0012 的原图字符有异常，9π/2 结合题意核算得出。
- Logo 原图存入 scrollferry/assets/logo.png，应用窗口图标和工具栏共用；macOS 启动检查正常。17 项本地测试通过，包括实际 PDF、上传断点续传、答案补正不改变素材标识和禁止修改已提交答案。

v1.0.2 验证：

- 填空题统一使用问答题页面，答案优先使用原图；原填空题号和本地题型保留。已将剩余 Q0016、Q0014、Q0013、Q0012、Q0011 按此顺序实际保存，没有录入推测公式文字；本批次队列为 completed，22 题均 saved。
- 所有新上传批次按清单倒序录题，试传及断点续传也采用同一顺序。历史 17 题没有重建，Q0015 仍是此前已保存的填空题。
- 五选项切图测试覆盖纵排和同行排列；后台空白单选题实测调用新增选项后有 5 个正确答案控件和 7 个编辑框，没有保存虚构测试题。动态选项支持 2–8 项。
- 20 项本地测试全部通过。GUI 标题使用用户指定的 ScrollFerry·舷渡-小鹅通题库上传工具-v1.0.2。
