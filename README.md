<p align="center"><img src="scrollferry/assets/logo.png" width="120" alt="ScrollFerry Logo"></p>

# 舷渡 ScrollFerry

**小鹅通题库上传工具 · v2.1.3**

ScrollFerry 是一个 Python 桌面工具，将 PDF 试卷拆分为题干、选项、答案和解析截图，再通过浏览器批量录入小鹅通题库。它面向需要保留数学公式、图表和原有排版的试卷，减少逐题截图、上传素材和插图的重复操作。

软件使用原 PDF 的渲染结果，不改写源文件。自动拆题后可以手动校对，规则识别不能保证任意版式均正确。

## 2.1.3 · 近两天更新总结

**更新范围：2026 年 9 月 23 日—9 月 24 日。** 本次集中完善分卷识别、截图校对、连续阅读和小鹅通上传，以下为这两天连续迭代后的最终行为。

### 识别更完整，答案匹配更准确

- 题目与答案解析可以分别导入；按“题型分区＋题号”匹配，解决不同题型都从第 1 题编号时答案冲突、漏匹配的问题。
- 补充名词解释、简答题、计算题、论述题及文字参考答案，支持五选项、同行选项、共用材料和写作题。
- 单题分区支持未编号的参考答案；主观题保留原卷答案截图，不强行提取为选择题字母。

### 截图更紧凑，减少误框与压字

- 题干、选项、答案和解析统一贴合内容，保留少量边距，减少大面积空白。
- 修正题号、选项标号的遮罩压住正文的问题，保留公式和上下标。
- 排除重复页眉、中文页码页脚，并以题型标题作为分界，修复简答题混入“计算题”标题、论述题选框延伸至页脚的问题。

### 原 PDF 连续阅读，跨页校对更直观

- 滚轮可直接浏览下一页和上一页；跨页内容显示“上一页尾部＋下一页头部”，两页保留各自的绿色选框和原 PDF 上下文。
- 点击题干、选项或解析自动定位选框；内容已在视野内时不再无必要地跳转。
- 修复 Tk 9 触控板和部分鼠标的滚动无响应，左侧面板、题目列表、PDF、截图预览及日志分别响应滚动。
- 同页切换截图复用 PDF 图像，只更新选框和遮罩，减少闪烁；跨页编辑使用对应页面的坐标。

### 界面、进度反馈与动画重新整理

- 统一“舷渡 ScrollFerry”中英文名称、蓝白配色和按钮层级，精简工具栏，增加菜单、快捷键、侧栏显隐与缩放。
- 任务窗口显示阶段、数量、耗时和日志；完成后保留结果，点击居中的“确定”才关闭。
- 小船动画加入蓝色水岸、济南建筑剪影、移动云彩和往返航行，修正透明穿帮与闪烁，缩小高度并铺满内容区宽度，加快移动速度。
- 任务停止时动画停止，并提供“减少动态效果”选项；动画不代表真实处理进度。

### 上传可恢复，减少重复操作

- 图片按每批最多 100 张上传并核对，保留已确认进度。
- 改进“从素材库选择”菜单展开，减少对手动悬停的依赖；修正素材分组搜索结果不是树节点时的定位超时，支持重新加载查找。
- 编辑器未加载完整时延长等待，并在保存前最多自动重开当前题目两次。
- 失败窗口增加“重试”，直接继续原批次，跳过已保存内容；分组创建和题目保存结果不明确时仍停止核对，避免重复提交。
- 上传界面改为选择上传范围，再点击统一的“开始上传”；导出完成窗口新增“上传到小鹅通”，直接使用刚导出的批次。

**本次验证：** 本地 59 项测试中 57 项通过、2 项外部样本测试默认跳过；另行检查的 396 分卷 57 题和 433 税务分卷 23 题均匹配答案解析。新增上传恢复逻辑尚未完成真实小鹅通端到端验收，Windows 安装包以 Actions 构建结果为准。

升级后请完全退出旧进程再启动。新拆题规则需要重新导入 PDF；恢复上传请使用原批次，不要用新导出的批次替代未完成批次。详细变更与升级说明见 [2.1.3 更新日志](CHANGELOG.md)。

## 文档导航

- [使用说明](docs/user-guide.md)：安装启动、截图校对、导出、上传和故障处理。
- [技术说明](docs/technical-guide.md)：模块结构、数据格式、上传状态及开发维护。
- [Windows 打包说明](docs/windows-packaging.md)：通过 GitHub Actions 生成安装 EXE。
- [集成验证记录](docs/xiaoe-integration.md)：历史实测过程，当前行为以代码和使用说明为准。

## 主要功能

- 自动识别带文字层 PDF 的题目和答案标记，分别截取题干、选项及解析。
- 支持“题目＋解析 PDF”分卷导入、按题型分区及题号匹配答案；共用材料复制到各小题，写作题以问答题处理。
- 保留公式和图片，遮盖原题号与选项标号，保留“解析”文字。
- 支持调整截图、擦除标号、追加跨页片段、撤销和重做。
- 支持 A–H 连续选项，包括五选项题；同行选项可分别切图。
- 每次导出新建批次，图片名称包含试卷名和批次编号。
- 先将整卷图片集中上传到专属素材分组，再创建或复用同名题库分类。
- 按导出清单倒序录题，按完整图片名搜索、插入并保存。
- 保留上传进度，恢复时跳过已保存题目；结果不确定时暂停核对。

| 本地题型 | 小鹅通题型 | 答案处理 |
| --- | --- | --- |
| 单选题 | 单选题 | 根据答案字母设置正确选项 |
| 多选题 | 多选题 | 根据多个答案字母设置正确选项 |
| 填空题 | 问答题 | 优先使用答案原图，其次已有文字答案 |
| 解答题 | 问答题 | 依次优先使用答案原图、文字答案、完整解析图 |

## 快速开始

### Windows 安装版

在仓库 Actions 中选择一次成功的 **Build Windows installer** 运行，下载附件 `ScrollFerry-Windows-Installer-2.1.3`。解压后运行 `ScrollFerry-Setup-2.1.3-x64.exe`。安装版包含 Python 和上传组件，电脑仍需安装 Chrome 或 Edge。

只有成功完成构建和安装检查的运行才提供附件；本文不代表已发布正式 Release，详见 [打包说明](docs/windows-packaging.md)。

### 源码运行

准备包含 Tcl/Tk 的 Python 3.11 或更新版本，推荐开发环境为 Python 3.12。

- macOS：双击 `start_mac.command`；需要上传时运行 `安装上传组件_mac.command`。
- Windows：双击 `start_windows.bat`；需要上传时运行 `安装上传组件_windows.bat`。

也可以在自己的虚拟环境中执行：

```bash
python -m pip install -r requirements.txt -r requirements-upload.txt
python -m scrollferry.app
```

上传使用软件独立的浏览器窗口，首次需要登录小鹅通。Windows 优先尝试默认的 Chrome 或 Edge，启动失败后尝试另一个；其他默认浏览器暂不支持直接接入。

## 一次完整操作

1. 从“导入文件”选择“打开 PDF”；答案单独成卷时选择“题目＋解析 PDF”。
2. 核对题数、题型、选项、答案和截图边界。滚轮可连续跨页，选中跨页内容可查看上下页选框。
3. 点击“导出截图”，等待完成后点击“上传到小鹅通”，直接使用刚导出的批次。
4. 选择“先试传一道题目”，点击“开始上传”。它仍会先上传整卷素材，并真实保存一题。
5. 检查结果后，选择“继续上传全部待处理题目”。失败时可用“重试”恢复；已保存题目会跳过，未确认的提交仍需核对。

上传期间保持软件及其专用浏览器窗口打开。重启后选择原批次的 `manifest.json` 可恢复上传。

## 数据存放

| 数据 | 保存位置 |
| --- | --- |
| 原 PDF、工程、导出截图 | 用户选择的位置 |
| Windows 登录资料、上传进度 | `%LOCALAPPDATA%\ScrollFerry` |
| macOS 登录资料、上传进度 | `~/Library/Application Support/ScrollFerry` |

恢复进度依赖原批次路径和本机用户数据，请保持目录位置不变。登录资料、实际试卷和运行输出不应上传到 GitHub。

## 当前边界

- 优先支持带文字层的单栏试卷；双栏、复杂表格、集中答案表和不规则编号可能需要调整或适配。
- 尚未接入扫描件 OCR，纯扫描件可能无法自动建立工程。
- 不提供 Word 转 PDF、钉钉验证或独立账号系统。
- 原 PDF 中已损坏或显示异常的公式不会自动修复。
- 小鹅通使用页面自动化，后台改版可能需要更新适配代码。
- 新规则不自动重建历史题目，也不重排已经保存的题目。

2.1.3 本地回归共 59 项测试，57 项通过、2 项外部样本测试默认跳过；另外检查了 396 分卷 57 题和 433 税务分卷 23 题的答案匹配。新增上传恢复逻辑使用替身测试，尚未完成真实小鹅通端到端验收；Windows 安装包状态以 Actions 为准。

## 开发与测试

```bash
python -m pip install -r requirements.txt -r requirements-upload.txt reportlab
python -m unittest discover -s tests -v
```

实际试卷回归需设置 `SCROLLFERRY_TEST_PDF` 指向本地样本，否则该项跳过。普通单元测试不登录小鹅通、不创建真实题目。Windows 安装包通过 GitHub 的 Windows runner 构建，具体架构和维护方式见 [技术说明](docs/technical-guide.md)。

## 许可

本项目采用自定义的 [个人学习使用许可协议](LICENSE)：**仅供个人、非商业学习使用，商用需另行取得书面授权**。

允许个人学习用途的阅读、运行、编译、修改和 Fork，并须保留许可及归属说明。企业内部运营、收费代上传、题库加工、商业产品集成等不属于个人学习用途；其他用途及安装包再分发须另行授权。请通过本仓库联系维护者申请授权。

本项目公开源码，但不采用 OSI 认可的开源许可证。第三方依赖遵循各自的许可证，完整条件以 `LICENSE` 为准。

## 免责声明

ScrollFerry 是独立开发的辅助工具，并非小鹅通官方产品，未声明获得小鹅通的认可或背书。

- 请仅处理和上传你拥有相应使用权限的试卷、图片及解析，并遵守所用平台的规则。
- 自动拆题、答案识别和页面操作可能出错。上传前应校对，上传后应检查题目数量、分类、答案和解析。
- 上传会实际创建素材和题目；取消操作不会撤回已保存内容。请保留源文件、工程和上传记录，避免重复录入。
- 软件不承诺适配所有 PDF、操作系统或后台页面，也不保证操作持续可用。使用前请先小批量验证。
- 浏览器资料中可能包含登录状态，请妥善保管，不要上传到公开仓库或分享给无关人员。
